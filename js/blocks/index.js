/* ============================================================
   blocks/index.js
   Registers every block type. To add a new block type, write its
   class (see base.js for the contract) and add one line here.
   ============================================================ */
BlockRegistry.register(QuestionnaireCounterBlock);
BlockRegistry.register(SleepBlock);
BlockRegistry.register(BottleBlock);
BlockRegistry.register(MealBlock);
BlockRegistry.register(DiaryBlock);
